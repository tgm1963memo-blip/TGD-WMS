# Seed README

## Purpose
Provide guidance for developers on how to **load** the demo seed data into a **staging** Supabase instance. This data is **never** for production use.

## Important notes
- **Do NOT execute** this seed file (`003_tgd_wms_seed_data_foundation.sql`) against a live production Supabase project.
- The data is **fictional** and contains only demo emails, UUIDs, and placeholder values.
- Dependencies:
  1. Migration `001_tgd_wms_schema_foundation.sql` must be applied first.
  2. `tgd_user_profiles` role model (Sprint 13D) must exist.
  3. RLS policy foundation (Sprint 13C) must be in place (but not applied).
- How it will be used later:
  - In CI/CD pipelines a staging Supabase instance will run `supabase db reset && supabase db push` then execute this seed SQL.
  - Integration tests will load the JSON fixture (`tgd_wms_seed_data_foundation.json`) to mock API responses.
- **Rollback:** To revert, simply drop the tables or reset the Supabase project to a clean state.
- **Staging‑only warning:** This file is meant for local development or a dedicated staging environment **only**.

---
*Generated by Antigravity.*
