# RLS Policies

## Purpose
This directory contains the Row Level Security (RLS) policy SQL files for the TGD WMS project. The policies are **foundational only** and must be reviewed and approved by the Controller before being applied to a live Supabase instance.

## Files
- `001_tgd_wms_schema_foundation.sql` – Initial schema migration (already applied in Sprint 13B).
- `002_tgd_wms_rls_policy_foundation.sql` – RLS policy foundation (this sprint). It enables row‑level security on all core tables and defines placeholder policy skeletons.

## Important Notes
- **Do NOT apply** these policies to production without explicit Controller approval.
- No RPC functions, triggers, or service_role usage are included in this sprint.
- Policies reference `auth.uid()` and the `tgd_user_profiles` table for role mapping.
- Future sprints will add column‑level policies, RPC‑driven write controls, and trigger implementations.
- Ensure that the `tgd_user_profiles` table exists and is populated before enabling RLS.

## Review Checklist
- [ ] Confirm that all tables listed in the migration are covered by an `ENABLE ROW LEVEL SECURITY` statement.
- [ ] Verify that no `CREATE FUNCTION` or `CREATE TRIGGER` statements are present.
- [ ] Ensure that the document includes the service‑role warning.
- [ ] Validate that forbidden sales‑order terminology does not appear.
- [ ] Confirm that the policy file is version‑controlled and reviewed.

## Next Steps
- After Controller sign‑off, run the SQL against a staging Supabase instance.
- Implement RPC functions for stock‑changing actions.
- Add column‑level RLS policies as needed.
- Write integration tests to verify role‑based access.

---
*Generated by Antigravity.*
