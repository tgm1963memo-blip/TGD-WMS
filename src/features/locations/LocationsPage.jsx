import { PageShell } from '../../components/ui/PageShell.jsx';

export function LocationsPage() {
  return (
    <PageShell
      title="Locations"
      description="Warehouse location area reserved for future storage structure setup."
    />
  );
}

