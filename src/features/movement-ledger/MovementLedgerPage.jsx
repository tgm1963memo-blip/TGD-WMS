import { PageShell } from '../../components/ui/PageShell.jsx';

export function MovementLedgerPage() {
  return (
    <PageShell
      title="Movement Ledger"
      description="Movement ledger area reserved for future inventory transaction records."
    />
  );
}

