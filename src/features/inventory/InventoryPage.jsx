import { PageShell } from '../../components/ui/PageShell.jsx';

export function InventoryPage() {
  return (
    <PageShell
      title="Inventory"
      description="Inventory view area reserved for future ledger-derived balances."
    />
  );
}

