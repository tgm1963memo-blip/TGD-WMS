const fs = require('fs'); const readline = require('readline'); async function find404() { const fileStream = fs.createReadStream('test-results/temp_trace_5/0-trace.network'); const rl = readline.createInterface({ input: fileStream, crlfDelay: Infinity }); for await (const line of rl) { if (!line.trim()) continue; const evt = JSON.parse(line); if (evt.type === 'resource-snapshot' && evt.snapshot.response && evt.snapshot.response.status === 404) { console.log('404 URL:', evt.snapshot.request.url); } } } find404();
