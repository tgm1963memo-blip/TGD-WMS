node_modules/
node_modules/.vite/
dist/

# Environment files (never commit secrets)
.env
.env.local
.env.*.local
.env.local.testing

# Supabase CLI temp cache
supabase/.temp/

# Local UAT seed execution artifacts
database/seeds/.temp_*.sql

# Playwright / UAT generated artifacts
test-results/
playwright-report/
uat-evidence/

# jsPDF's save() writes directly to disk under Vitest's Node/jsdom test
# environment (no real browser download prompt to intercept there) --
# invoice-draft-pdf-export.test.js runs the real export end-to-end, which
# drops stray .pdf files at the repo root each run.
/*.pdf
.vercel
