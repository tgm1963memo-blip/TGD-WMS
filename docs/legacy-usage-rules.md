# Legacy Usage Rules

- Old `App.jsx` may be read for requirement discovery only.
- Do not refactor legacy files.
- Do not copy large blocks from legacy files.
- Do not import legacy code into `src/`.
- Any legacy insight must be rewritten as documentation first.
- Legacy behavior must be reimplemented intentionally inside the new architecture.
- Legacy files must remain isolated from the new runtime.

