const fs = require('fs'); const readline = require('readline'); async function findResponses() { const fileStream = fs.createReadStream('test-results/temp_trace_9/0-trace.network'); const rl = readline.createInterface({ input: fileStream, crlfDelay: Infinity }); let reqId = null; for await (const line of rl) { if (!line.trim()) continue; const evt = JSON.parse(line); if (evt.type === 'resource-snapshot') { if (evt.snapshot.response && evt.snapshot.response.status === -1) { console.log('-1 URL:', evt.snapshot.request.url); } } } } findResponses();
