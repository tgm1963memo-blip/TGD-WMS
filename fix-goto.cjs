const fs = require('fs'); const path = require('path'); const glob = require('glob'); const files = glob.sync('tests/e2e/**/*.spec.js'); for (const file of files) { let content = fs.readFileSync(file, 'utf8'); if (content.includes('page.goto(')) { content = content.replace(/import \{([^}]+)\} from '([^']+helpers\/uatAuth\.js)'/, (match, p1, p2) => { if (!p1.includes('gotoUrl')) { return \import { \, gotoUrl } from '\'\; } return match; }); content = content.replace(/await page\.goto\(/g, 'await gotoUrl(page, '); fs.writeFileSync(file, content); console.log('Updated ' + file); } }
