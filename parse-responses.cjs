const fs = require('fs'); const readline = require('readline'); async function findResponses() { const fileStream = fs.createReadStream('test-results/temp_trace_6/0-trace.network'); const rl = readline.createInterface({ input: fileStream, crlfDelay: Infinity }); let reqId = null; for await (const line of rl) { if (!line.trim()) continue; const evt = JSON.parse(line); if (evt.type === 'resource-snapshot' && evt.snapshot.request.url.includes('tgd_user_profiles')) { console.log('Request:', evt.snapshot.request.method, evt.snapshot.request.url); if (evt.snapshot.response) { console.log('Response status:', evt.snapshot.response.status); } else { console.log('No response field in snapshot!'); } } } } findResponses();
